import Loader from "react-loader-spinner"
import React, { Component } from "react"

class OnLoader extends React.Component {
    render() {
        return <Loader type="Puff" color="#00BFFF" height={100} width={100} />
    }
}
export default OnLoader
